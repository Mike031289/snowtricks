{% extends 'base.html.twig' %}
{% block title %}
    Accueil - SnowTricks
{% endblock %}
{% block body %}
    {# ================= HERO BANNER ================= #}
    <section class="hero-banner text-center text-white">
        {# Hero background image #}
        <img src="{{ asset('images/snow-banner.jpg') }}" alt="Montagne de snowboard" class="hero-img" loading="eager" fetchpriority="high">
            {# Hero overlay content #}
            <div class="hero-overlay d-flex align-items-center justify-content-center">
                <div class="hero-content">
                    <h1 class="display-4 fw-bold">
                        ❄ SnowTricks
                    </h1>
                    <p class="lead">
                        Explore, partage et maîtrise les tricks de snowboard
                    </p>
                    {# Show add button only for authenticated users #}
                    {% if is_granted('ROLE_USER', app.user) %}
                        <a href="{{ path('app_trick_new') }}" class="btn btn-light mt-3">
                            <i class="bi bi-plus-circle"></i>
                            Ajouter un trick
                        </a>
                    {% endif %}
                </div>
            </div>
        </section>
        {# ================= TRICKS LIST ================= #}
        <div class="container mt-5">
            <h2 class="text-center mb-5 fw-bold">
                Derniers Tricks
            </h2>
            <div class="row g-4">
                {# Loop through tricks #}
                {% for trick in tricks %}
                    <div class="col-md-4">
                        <div class="card trick-card h-100 shadow-sm">
                            {# Trick image #}
                            <div class="trick-image">
                                <img src="{{ asset('uploads/images/' ~ trick.mainImage) }}" alt="{{ trick.name }}"></div>
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center">
                                        {# Trick title #}
                                        <h5 class="card-title mb-0">
                                            {{ trick.name }}
                                        </h5>
                                        {# Action icons (UI only) #}
                                        <div class="d-flex gap-2 align-items-center">
                                            {# View trick #}
                                            <a href="{{ path('app_trick_show', {slug: trick.slug}) }}" class="text-primary" title="Voir le trick">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            {# Edit trick (secured by voter) #}
                                            {% if is_granted('TRICK_EDIT', trick) %}
                                                <a href="{{ path('app_trick_edit', {id: trick.id}) }}" class="text-warning" title="Modifier le trick">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                            {% endif %}
                                            {# Delete trick (secured by voter + CSRF) #}
                                            {% if is_granted('TRICK_DELETE', trick) %}
                                                <button type="button" class="btn btn-link text-danger p-0" data-bs-toggle="modal" data-bs-target="#deleteModal">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {# ================= DELETE MODAL (SINGLE INSTANCE OUTSIDE LOOP) ================= #}
                        <div class="modal fade" id="deleteModal" tabindex="-1">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    {# Modal header #}
                                    <div class="modal-header">
                                        <h5 class="modal-title">
                                            Confirmation de suppression
                                        </h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                    </div>
                                    {# Modal body #}
                                    <div class="modal-body">
                                        <p class="mb-0">
                                            ⚠️ Es-tu sûr de vouloir supprimer cette figure ?
                                            <br>
                                                Cette action supprimera définitivement toutes les données associées.
                                                <strong>
                                                    Cette action est irréversible.
                                                </strong>
                                            </p>
                                        </div>
                                        {# Modal footer #}
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                                Annuler
                                            </button>
                                            {# Secure delete form (CSRF protected) #}
                                            {% if is_granted('TRICK_DELETE', trick) %}
                                                <form method="post" id="deleteForm" action="{{ path('app_trick_delete', {id: trick.id}) }}" class="d-inline">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
                                                        <button class="btn btn-danger">
                                                            Supprimer
                                                        </button>
                                                    </form>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            {% else %}
                                <p class="text-center">
                                    Aucun trick disponible.
                                </p>
                            {% endfor %}
                        </div>
                    </div>
                    {# ================= PAGINATION ================= #}
                    {% if totalPages > 1 %}
                        <nav class="mt-5">
                            <ul class="pagination justify-content-center">
                                {# Previous page #}
                                <li class="page-item {{ currentPage == 1 ? 'disabled' }}">
                                    <a class="page-link" href="{{ path('app_home', {page: currentPage - 1}) }}">
                                        ←
                                    </a>
                                </li>
                                {# Page numbers #}
                                {% for i in max(1, currentPage - 2)..min(totalPages, currentPage + 2) %}
                                    <li class="page-item {{ currentPage == i ? 'active' }}">
                                        <a class="page-link" href="{{ path('app_home', {page: i}) }}">
                                            {{ i }}
                                        </a>
                                    </li>
                                {% endfor %}
                                {# Next page #}
                                <li class="page-item {{ currentPage == totalPages ? 'disabled' }}">
                                    <a class="page-link" href="{{ path('app_home', {page: currentPage + 1}) }}">
                                        →
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    {% endif %}
                {% endblock %}
                